# Data files are downloaded or generated by main.ipynb, so they are not committed.
# scripts/config.py creates the data folders when the notebook runs.
data/

# Python and Jupyter
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Spark
spark-tmp/
spark-warehouse/
metastore_db/
derby.log

# LaTeX build files (the PDF is kept)
report/*.aux
report/*.bbl
report/*.bcf
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.run.xml
report/*.synctex.gz
report/*.toc
# biber leaves this behind when it cannot write the .bbl
report/*.bbl-SAVE-ERROR

# macOS and editors
.DS_Store
.vscode/
.idea/


